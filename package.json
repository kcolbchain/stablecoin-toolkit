{
  "name": "@kcolbchain/stablecoin-toolkit",
  "version": "0.1.0",
  "description": "Open-source stablecoin infrastructure — issuance, reserve management, compliance, multi-geography support",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.0.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "license": "MIT"
}
